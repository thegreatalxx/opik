---
headline: Agent Configuration Overview | Opik Documentation
og:description: An overview of Opik's Agent Configuration system for versioning, managing,
  and deploying your agent's prompts and parameters without code changes
og:site_name: Opik Documentation
og:title: Agent Configuration Overview — Opik
title: Agent Configuration Overview
---

Agent Configuration is Opik's system for versioning and managing everything that influences your agent's behavior — prompts, model parameters, tool definitions, and more — outside of your codebase.

## Why use Agent Configuration

When building production agents, the parameters that matter most — prompts, temperatures, tool descriptions — are often buried in source code. Every time you want to tweak a prompt or adjust a temperature, you need a full code deploy. There's no audit trail of what changed between versions, and no easy way to roll back if something goes wrong.

Agent Configuration solves this by treating these parameters as versioned, deployable artifacts. You define them once in code, then manage and iterate on them through the Opik platform — no redeployment required.

## Key features

- **Version control**: Every configuration change creates an immutable version with a full audit trail
- **Environment labels**: Promote a version to `prod` or `staging` through the UI without touching code
- **Prompt management**: Track and version text prompts and multi-turn chat prompts with variable substitution
- **Typed parameters**: Store model names, temperatures, tool definitions, and any numeric or string values alongside your prompts
- **SDK support**: First-class Python and TypeScript SDK support for defining and fetching configurations

## How it works

At a high level, Agent Configuration works in two steps:

1. **Define your configuration** — Use the SDK to declare a `Config` subclass (Python) or a plain object (TypeScript) with your fields and default values, then push the first version with `create_config` / `createConfig`.
2. **Fetch at runtime** — Your agent calls `get_or_create_config` (Python) or `getOrCreateConfig` (TypeScript) inside a tracked function. Opik returns the matching configuration for the requested environment or version. If the backend is temporarily unreachable, the SDK falls back to the values you provided — so your agent keeps running.

When you want to update your agent's behavior, create a new version in the Opik UI or SDK and move the `prod` label to it. Your running agent picks up the change on its next fetch — no redeployment needed.

## Next steps

- [Getting started](/prompt_engineering/getting-started) — Add Agent Configuration to your code in minutes
- [Concepts](/prompt_engineering/concepts) — Deep dive into versions, environments, and configuration types
